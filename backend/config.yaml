server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 3306
  user: root
  password: 
  dbname: user_admin
  charset: utf8mb4

jwt:
  secret: employee-management-secret-key
  expire: 24h
